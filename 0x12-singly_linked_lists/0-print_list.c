#include "lists.h"

/**
 *
 *
 *
 */

size_t print_list(const list_t *h)
{
	size_t count = 0;

	if(h == NULL)
		return (0);

	h -> next != 0;

	if (h -> str == NULL)
	{
		printf("[%u] and %s\n" h -> len, h -> str;
				count ++;
	}
	return (count);
}
