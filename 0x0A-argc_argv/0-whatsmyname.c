#include<stdio.h>

/**
 * main - Entry point
 * @argc: number of parameters
 * @argv: array of parameters
 *
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);

	return (0);
}
